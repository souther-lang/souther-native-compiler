//! What a Souther program calls that is not its own code.
//!
//! Empty while the first programs compiled here are arithmetic over `Int`, which generated code
//! does itself. What arrives first is what a kernel call needs, and what is written here is written
//! against [`souther_native_abi`] so that neither side states a layout the other also states.
//!
//! Where a computation here turns out to be the one the wasm runtime already does — a calendar,
//! a regular expression — it is lifted into something both read. That is done when the second
//! copy exists and not before: until then there is nothing to tell a shared meaning from a shared
//! spelling.
