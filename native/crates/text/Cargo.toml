[package]
name = "souther-text"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Depends on nothing, and nothing of the native backend: this is what the language says text means,
# over bytes, and whatever it reached into here would be one more thing to carry when it moves out
# to what both runtimes read (#17).
[dependencies]
