disallowed-methods = [
    { path = "std::collections::HashMap::insert", reason = "put a name into an index through `index::once` or `index::unique`, which refuse a key written twice" },
    { path = "std::collections::BTreeMap::insert", reason = "put a name into an index through `index::once` or `index::unique`, which refuse a key written twice" },
]
